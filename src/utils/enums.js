/**
 * Application-level enums (not generated from Prisma)
 * Use these for validation, constants, and business logic
 */

export const IncomeRange = {
  WEEKLY: "weekly",
  MONTHLY: "monthly",
};

export const IncomeRangeValues = Object.values(IncomeRange);
